/* ═══════════════════════════════════════════════════════════
   config_map.js — 지도 키 한 곳
   ----------------------------------------------------------------
   여기 값 하나만 채우면 '그날 지도'와 '지도에서 주소 찍기'가 함께 켜진다.
   비어 있으면 두 기능이 조용히 꺼진 채로 남고, 앱의 나머지는 아무 영향이 없다.

   ⚠️ 카카오는 키가 네 종류다(REST · JavaScript · Admin · 네이티브).
      여기 쓰는 건 **JavaScript 키**다. REST 키를 넣으면 지도가 안 뜬다.

   ⚠️ 이 값은 비밀이 아니다 — 파이어베이스 apiKey 와 같은 성격이라 공개 저장소에
      그대로 적어도 된다. 카카오는 키가 아니라 **도메인**으로 막는다.
      그래서 아래 도메인 등록을 안 하면 키를 넣어도 지도가 안 뜬다.

   키를 바꿔야 할 때 (카카오 개발자 콘솔, developers.kakao.com):
     찍고쓰다 앱 → 앱 설정 → 앱 → 플랫폼 키 → JavaScript 키.
     그 키의 'JavaScript SDK 도메인' 에 https://localhost 가 들어 있어야 한다.
     (PC 에서 npx serve 로 볼 일이 있으면 http://localhost:3000 도 같이 등록)
     주소→좌표 변환도 이 키 하나로 된다 — REST 키나 별도 신청이 필요 없다.

   ☠️ 안드로이드 앱의 주소가 https://localhost 인 이유:
      Capacitor 는 웹 자산을 APK 안에 넣고 WebView 에 https://localhost 로 띄운다.
      그래서 카카오 입장에서는 '그 도메인에서 온 요청'으로 보인다.
      찍고쓰다도 같은 주소를 쓴다 — 아래에서 그 앱 키를 그대로 쓰는 근거가 이것이다.
═══════════════════════════════════════════════════════════ */
/* ☠️ 2026-09-17 — 이 값은 **찍고쓰다 앱(카카오 앱 ID 1561464)의 JavaScript 키**다.
      현장매니저용 카카오 앱(ID 1581158)을 따로 만들어 두긴 했지만 쓰지 않는다.
      카카오가 무료 쿼터를 **개발자 계정당 첫 번째로 카카오맵을 켠 앱 하나에만** 주는데,
      그 하나를 찍고쓰다가 이미 가져갔다. 두 번째 앱부터는 비즈월렛을 연결해야 켜진다.
      두 앱 모두 안드로이드 WebView 주소가 https://localhost 로 같아서, 찍고쓰다 앱에
      이미 등록된 이 키가 여기서도 도메인 검사를 통과한다.
   ⚠️ 대신 두 앱 사용량이 한 카카오 앱에 섞이고 무료 쿼터를 나눠 쓴다.
      사용량은 콘솔 '통계 → 기간별 사용량'(찍고쓰다 앱)에서 본다. 많이 쓰게 되면
      현장매니저 앱(1581158)에 월렛을 연결해 이 값만 그 앱 키로 바꾸면 분리된다. */
window.KAKAO_JS_KEY = '7c6b3a6ba30f09cf55bbe5e82c4859f1';

/* ═══════════════════════════════════════════════════════════
   실제 도로 경로 (2026-09-18) — 아직 꺼져 있다
   ----------------------------------------------------------------
   비워 두면 그날 지도가 지금처럼 **점선(직선)** 으로 순서만 보여 준다.
   여기에 서버 함수 주소를 넣으면 내 위치에서 출발해 그날 일정을 순서대로 잇는
   **실제 도로 경로**가 실선으로 그려지고, 총 거리·소요시간이 머리줄에 뜬다.

   켜는 순서
     ① developers.kakaomobility.com 에서 길찾기 API 사용 신청 → REST 키 발급
        (지도 JS 키와 **다른 서비스**다. 위 키로는 안 된다)
     ② functions/index.js 에 경로 프록시 함수 추가 + 키는 서버 시크릿에 저장
     ③ 배포된 함수 주소를 여기에 붙여넣기

   ☠️ REST 키를 여기(앱)에 직접 넣지 말 것.
      위 지도 JS 키는 **도메인**으로 막혀 있어 공개돼도 괜찮지만, REST 키에는 그 장치가 없다.
      APK 에서 꺼내면 누구나 쓸 수 있고 쿼터는 이 계정에서 나간다.
      그래서 서버를 한 번 거친다 — functions 의 adminStats 가 앤트로픽 키를 다루는 방식과 같다.
   ⚠️ 주고받는 모양은 www/js/routing.js 주석에 적혀 있다. 서버를 만들 때 그대로 맞출 것.
═══════════════════════════════════════════════════════════ */
window.KAKAO_ROUTE_URL = 'https://asia-northeast3-work-report-826ec.cloudfunctions.net/naviRoute';
